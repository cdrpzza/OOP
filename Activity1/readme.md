Activity 1

M6 Exercise 1
31. Which arithmetic operator performs the salary calculation?

The arithmetic operator that performs the salary calculation is the multiplication operator or the asterisk (*).

32. Why should hours worked be allowed to contain decimals?

Hours worked should be allowed to contain decimals because when entering a time with a minute, for example 7 hours and 30 minutes, users can enter it as 7.5 hours instead.
 
33. Write the Java expression for gross salary.

gross_salary = hours_worked * hourly_rate;

M6 Exercise 2
34. What operator calculates the subtotal?

The operator that calculates the subtotal is the multiplication operator or the asterisk (*).

35. Why is 0.12 used for 12% VAT?

0.12 is used for the vat because when 12 is divided by 100, the result will become 0.12. Also 0.12 is the decimal equivalent off 12 percent. 

37. Write the three formulas used by the program.

    subtotal = price * quantity;
        vat = subtotal * VAT_RATE;
        final_total = subtotal + vat;


M6 Exercise 3
37. Why should 9.0 and 5.0 be used instead of only integer values?

9.0 and 5.0 should be used since it can indicate to Java that the calculation is done using a double data type.

38. What arithmetic operators are used in the formula?

The arithmetic operators used in the formula are the multiplication or asterisk (*), division or the slash sign (/), and addition or plus sign (+).

39. What is the expected result when Celsius is -40?

The expected result when celsius is -40 is -40 degrees fahrenheit.


M6 Exercise 4
40. Why are parentheses useful in the BMI expression?

Parentheses are useful in BMI expression because they help us to compute for needed values first such as the height * height, which will then be divided to weight after computing for the height of the user.

41. Which operator is evaluated inside the parentheses?

The multiplication operator or asterisk (*) is evaluated inside the parentheses.

42. Write the Java expression for BMI.

bmi = weight / (height * height);

M6 Exercise 5
44. Why must the weights add up to 1.00?

It must add up to 1.00 because when multiplied to 100, the result becomes 100 percent which is the maximum grade that a student can get.

45. What is the contribution of an exam grade of 85 at 40%?

The contribution of an exam grade of 85 at 40 percent is 34.

46. Write the full Java expression for finalGrade.

grade = (quiz * QUIZ_WEIGHT) + (project * PROJECT_WEIGHT) + (exam * EXAM_WEIGHT);
